<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE EmailTemplate PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<EmailTemplate name="Alpha-EmailTemplate-TerminationFailure">
  <Body>

    Hi Team,

    The user #if($targerIdentity.getDisplayName()) $targerIdentity.getDisplayName() #end termination is Failed. Refer to the below log from IIQ.

  </Body>
  <Description>
    Email to Notify IIQ Operations team for User Termination Failure.
  </Description>
  <Signature>
    <Inputs>
      <Argument name="emailArgs" type="Map">
        <Description>Map holding input agrumenst.</Description>
      </Argument>
      <Argument name="targerIdentity" type="string">
        <Description>The Identity of terminated user.</Description>
      </Argument>
    </Inputs>
  </Signature>
  <Subject>User #if($targerIdentity.getDisplayName()) $targerIdentity.getDisplayName() #end has terminated</Subject>
</EmailTemplate>