<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Workflow PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Workflow explicitTransitions="true" libraries="Identity,IdentityRequest" name="Alpha-Workflow-Identity Request Finalize" type="Subprocess">
  <Variable name="project">
    <Description>ProvisioningProject which is just a compiled version of the ProvisioningPlan.
      From the project the IdentityRequestItem model will be updated.</Description>
  </Variable>
  <Variable input="true" name="identityRequestId" output="true">
    <Description>The ID ot the identitytRequestId.

      This step will update the identityRequest status and completion
      status.</Description>
  </Variable>
  <Variable input="true" name="approvalSet">
    <Description>This attributes is set during the &amp;quot;Build Approval Set&amp;quot; step,
      which builds this list by going through the ProvisioningPlan
      to build the line items that need to be approved,

      This variable includes all ApprovalItems that are part of
      the request process and is updated during the AfterScript
      of the approval process by assimilating the decisions
      and comments from the Approvals copy of the ApprovalItem.</Description>
  </Variable>
  <Variable input="true" name="trace">
    <Description>Used for debugging this subprocess and when set to true trace
      statements will be sent to stdout.</Description>
  </Variable>
  <Variable input="true" name="ticketManagementApplication">
    <Description>Name of the application that can handle ticket requests.
      When non-null the Manage Ticket Steps will be visited to open
      tickets during the workflow lifecycle.</Description>
  </Variable>
  <Variable input="true" name="ticketDataGenerationRule">
    <Description>A rule that builds up the ticketing provisioning plan based on the workflow
      context.  This rule gets all of the arguments to this subprocess
      plus the curreent workflow context.  The rule can return either a
      ProvisioningPlan or a Map of name value pairs.

      The rule name can be specified here on the subprocess or on the
      ticketManagementApplication config under the same name as this
      variable.</Description>
  </Variable>
  <Variable name="autoVerifyIdentityRequest">
    <Description>Flag to indicate when finishing the request we should
      automatically mark it verified. Currently used by
      the change password workflow so we don&amp;#39;t wait to
      verify since passwords can&amp;#39;t be verifed.</Description>
  </Variable>
  <Variable input="true" name="workItemPriority">
    <Description>String version of WorkItem.level that was used
      to set the priority of the IdentityRequest and
      workitems.</Description>
  </Variable>
  <Variable input="true" name="failureAction"/>
  <Variable input="true" name="alphaWorkflowFailureMap"/>
  <RuleLibraries>
    <Reference class="sailpoint.object.Rule" name="Alpha-Rule-Joiner-RuleLibrary"/>
    <Reference class="sailpoint.object.Rule" name="Alpha-Rule-AuditEvent-Library"/>
    <Reference class="sailpoint.object.Rule" name="Alpha-Rule-Generic-RuleLibrary"/>
  </RuleLibraries>
  <Step icon="Start" name="Start" posX="16" posY="12">
    <Transition to="Send Failure Emails">
      <Script>
        <Source><![CDATA[
          import org.apache.log4j.Logger;

          Logger log = Logger.getLogger("iiq.alpha.workflow.Alpha-Workflow-Identity Request Finalize");

          log.debug("Entering Workflow step: "+wfcontext.getStep().getName());
          Map map = wfcontext.getVariable("alphaWorkflowFailureMap");
          log.debug("alphaWorkflowFailureMap:"+alphaWorkflowFailureMap);
          if(null!=map && map.size()>0)
          return true;
          else
          return false;]]></Source>
      </Script>
    </Transition>
    <Transition to="Audit Completion"/>
  </Step>
  <Step action="call:auditLCMCompletion" icon="Audit" name="Audit Completion" posX="242" posY="7">
    <Arg name="approvalSet" value="ref:approvalSet"/>
    <Transition to="Complete Identity Request"/>
  </Step>
  <Step action="call:completeIdentityRequest" icon="Task" name="Complete Identity Request" posX="369" posY="8" resultVariable="identityRequest">
    <Arg name="autoVerify" value="$(autoVerifyIdentityRequest)"/>
    <Arg name="approvalSet" value="ref:approvalSet"/>
    <Arg name="project" value="ref:project"/>
    <Arg name="identityRequestId" value="ref:identityRequestId"/>
    <Transition to="Update Ticket On Complete"/>
  </Step>
  <Step condition="script:(ticketManagementApplication != null)" icon="Task" name="Update Ticket On Complete" posX="482" posY="8">
    <Arg name="trace" value="ref:trace"/>
    <Arg name="ticketManagementApplication" value="ref:ticketManagementApplication"/>
    <Arg name="project" value="ref:project"/>
    <Arg name="action" value="complete"/>
    <Arg name="workItemPriority" value="ref:workItemPriority"/>
    <Arg name="identityRequestId" value="ref:identityRequestId"/>
    <Arg name="source" value="ref:source"/>
    <Arg name="ticketProject"/>
    <Arg name="ticketId"/>
    <Arg name="ticketDataGenerationRule"/>
    <Arg name="ticketPlan"/>
    <Description>
      Call a subprocess to update the ticket in the ticketManagementApplication is non-null.

      You can specify a specific 'ticketDataGenerationRule' here or you can also specify
      it on the application.  It'll be read from the argument first and fall back to the '
      application config.
    </Description>
    <WorkflowRef>
      <Reference class="sailpoint.object.Workflow" name="Manage Ticket"/>
    </WorkflowRef>
    <Transition to="end"/>
  </Step>
  <Step icon="Stop" name="end" posX="618" posY="12"/>
  <Step action="rule:Alpha-Rule-LCE-FailureAlerting-WorkFlow Rule" name="Send Failure Emails" posX="82" posY="121">
    <Arg name="identityName" value="ref:identityName"/>
    <Arg name="project" value="ref:project"/>
    <Description>Check is the Workflow is failed, if yes, send Notifications and Audit.</Description>
    <Transition to="Auditing Workflow Failure"/>
  </Step>
  <Step icon="Audit" name="Auditing Workflow Failure" posX="241" posY="121" resultVariable="auditEventId">
    <Arg name="identityName" value="ref:identityName"/>
    <Arg name="failureAction" value="ref:failureAction"/>
    <Arg name="auditEventId" value="ref:auditEventId"/>
    <Script>
      <Source><![CDATA[import sailpoint.object.Identity;
        import org.apache.log4j.Logger;

        Logger log = Logger.getLogger("iiq.alpha.workflow.Alpha-Workflow-Identity Request Finalize");

        log.debug("Entering Workflow step: "+wfcontext.getStep().getName());
        log.debug("identityName"+identityName);
        log.debug("failureAction"+failureAction);
        Identity identity = (Identity) context.getObject(Identity.class, identityName);
        String action = failureAction;
        String targetIdentity = identity.getDisplayName();
        return createAuditEvent(wfcontext.getWorkflow().getName(), targetIdentity, action,null,null,null,null,null,null,null,null,null,null);
      ]]></Source>
    </Script>
    <Transition to="Audit Completion"/>
  </Step>
</Workflow>